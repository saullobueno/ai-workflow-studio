# Chave da API Groq (gratuita, sem cartão de crédito: https://console.groq.com/keys)
# usada pelo endpoint serverless do AI Copilot (api/copilot.ts).
# Nunca prefixe com VITE_: essa variável só pode existir no lado servidor.
GROQ_API_KEY=
